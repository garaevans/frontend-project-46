extends: 'airbnb-base'

parserOptions:
  ecmaVersion: 2020

rules:
  no-console: 'off'
  import/extensions:
    - error
    - ignorePackages
    - js: always
  no-underscore-dangle: [2, { "allow": ["__filename", "__dirname"] }]
